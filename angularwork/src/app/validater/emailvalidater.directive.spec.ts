import { EmailvalidaterDirective } from './emailvalidater.directive';

describe('EmailvalidaterDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailvalidaterDirective();
    expect(directive).toBeTruthy();
  });
});
